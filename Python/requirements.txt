# Core dependencies for ICCG Python binding
numpy>=1.19.0
scipy>=1.5.0
pybind11>=2.6.0

# Development and testing dependencies
pytest>=6.0.0
pytest-cov>=2.10.0
matplotlib>=3.3.0

# Build dependencies (for setuptools)
setuptools>=50.0.0
wheel>=0.36.0